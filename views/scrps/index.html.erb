<p id="notice"><%= notice %></p>

<h1>Scrps</h1>
<p><%= Time.now%></p>
<p><%= @vls %><p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>

    <%# <%= @elements.inner_text %> %>
    <br />
    <br />

    <% @scrps.each do |scrp| %>
      <tr>
        <td><%= scrp.name %></td>
        <td><%= link_to 'Show', scrp %></td>
        <td><%= link_to 'Edit', edit_scrp_path(scrp) %></td>
        <td><%= link_to 'Destroy', scrp, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Scrp', new_scrp_path %>
